# 集体电子跃迁：后续低内存泊松方向的算子检查

这是针对 30/15 长时噪声的研究记录，不是已接入生产的算法，也没有新的 50/25、4000 a.u. 高精度结果。当前长任务仍使用 v1.13/v1.14。

## 为什么应处理电子轨道求和

星形跃迁 H1 = d† Σ_j c_j a_j + (Σ_j c_j a_j)† d。定义 g²=Σ_j |c_j|²，b=(Σ_j c_j a_j)/g，则 H1=g(d†b+b†d)。将 d、b 变为两个正负组合轨道后，H1=g(n_+−n_−)。在 0<Ne<Norb 的固定电子数空间，其谱范数为 g，不随填充增长。

当前模型所有浴耦合相同且 g=√eta；但逐浴轨道抽样的现有实现使用 Λ=√[Ne(Norb−Ne)] |c|，提议速率 r=rate_scale×Λ。半填充时 Λ/g 约随 √Norb/2 增长。现有 rate_scale=1.5、T=4000 时，连续近似平均事件数对 10/5、30/15、50/25 分别约 5.48、9.15、11.74；直接按集体算子范数选 r=g 时均约 2.19。代码的有限步事件数实际上服从相应二项分布，均值略小于 rT。

这解释了为什么在固定总耦合强度下，逐轨道采样仍可能随电子数增加而更难收敛。不能只凭事件数较小就推断所需样本数或运行时间按同样倍数下降。

## 保留泊松结构的方式

核相干态仍在给定杂质占据的谐势上解析传播，泊松事件时间仍随机采样。每次电子事件直接应用整个 H1，将浴轨道求和包含在一个 Slater 行列式更新中，而不再随机选一个浴轨道标签。条件杂质占据在每次事件后翻转；这使路径上的电子态仍可表示为一个 Slater 行列式。

对连续时间、无参考控制的这一候选表示，若 H0 传播是幺正的，泊松波函数路径二阶矩有上界 exp[(r+g²/r)T]，在 r=g 时为 exp(2gT)。独立前后路径对估计范数不超过 1 的观测量时，可得到 exp(4gT) 的二阶矩上界；当前参数约 6.4e3。这个上界仍很大，完全不能直接保证 1e-8 量级误差，也不能套用到未修改的 Kondo 条件采样器。因此仍需要合适的控制变量或解析求和来处理微弱物理信号。

一个 50×25 复数双精度轨道矩阵仅为 20000 字节，但这只是一个电子路径组件，不是程序实际 RSS。QR、重叠矩阵、核信息、输出和参考控制仍占内存；每次 QR/SVD 的计算量也比单个组态跳跃大。

## 已验证与未解决部分

既有 Slater 原型的小空间逐组态与相干核检查见 SLATER_KERNEL_PROTOTYPE.md。新增 tests/test_collective_jump_norm.py 用费米反对易关系给出的独立范数恒等式，检查随机复杂轨道：杂质已占时，归一化跃迁范数平方为 1−||Q†c_hat||²；杂质为空时为 ||Q†c_hat||²。

共 448 个随机态，覆盖 10/5、30/15、50/25、50/17、64/32、50/1、50/49。最大范数恒等式误差约 1.03e-14，同时检查跳跃后的杂质占据和轨道正交性。结果保存在 scan_rate_4000_20260907/collective_jump_norm_validation.json。原型目前只接受实耦合；不能声称生产程序已支持任意复耦合或电子双体相互作用。

关键未解决问题是控制变量：把 Slater 态投影到当前有界参考空间，一般不再是单个 Slater 行列式。因此不能直接替换现有逐组态跳跃就宣称估计器仍正确。需要重新推导控制变量，例如按事件数组织匹配的 Dyson 参考，并验证跨参考项、有限步偏差、所有观测量和实际长程方差。这些工作尚未完成。
