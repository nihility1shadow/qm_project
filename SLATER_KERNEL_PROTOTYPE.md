# Slater 条件路径原型：已核验的范围与限制

这是独立研究原型，尚未接入正式 Poisson 模拟。正式长测仍使用 v1.13；这些核验分数不能当作 Anderson–Holstein 模型的拟合指数。

## 已实现并核验

- 固定杂质占据数的电子条件态用一个 Norb × Nel 的 Slater 矩阵表示。
- 对星形耦合，在每次杂质占据数翻转时，将全部电子轨道跳跃精确求和。QR 分解保持矩阵稳定，行列式相位和费米子符号吸收到复权重中。
- 电子重叠和单轨道占据矩阵元使用伴随矩阵形式；即使两个 Slater 态的重叠为零，仍正确保留可能非零的单体矩阵元。
- 给定核条件路径，用相干态解析传播位移谐振子，并跟踪相位。
- 对连续时间泊松路径，联合时间密度为 exp(-rT) r^k，路径权重为 exp(rT) (-i/r)^k 乘各次 H1 与区间 H0 传播。两个独立路径的矩阵元给出未归一化密度估计。实际蒙特卡洛方差尚未测量。

电子核验覆盖 44 个小 Hilbert 空间、1760 次条件态重建、881 次矩阵元检查和 880 次精确 H1 分支检查；最大误差约 9e-15。与逐项创建/湮灭的独立全空间矩阵比较，而非仅比较自身重复实现。

核条件路径覆盖 0–4000 a.u. 的 4 种跳跃历史、17 个自由传播区间。质量、频率、位移与本项目相同。独立 Fock 参考需要按整个区间的最大振幅选取截断，最终使用 2840 层；最大波函数差异 9.59e-13、重叠差异 3.77e-14。这个较大的稠密参考仅用于核验，原型单条核路径本身只保存一个复数相干态参数。构造参考展开时使用对数系数，避免远离原点的相干态首项下溢。

另以关闭 H0 的纯跃迁模型核验泊松计数权重：将 0–24 次跳跃确定性求和，与单体矩阵指数产生的精确 Slater 态比较，覆盖 4/1、6/3、10/5、30/15，时间参数为 4000 a.u.。30/15 最大占据数差异 1.28e-14。这个测试关闭了实际核和轨道自由演化，绝不能作为真实 30/15 模型的 4000 a.u. 成功结果。

30/15 单个条件电子矩阵占 7200 字节。这个数只描述矩阵本身，不代表程序总内存、双路径缓存或方差控制所需内存。

## 仍需解决

1. 当前正式算法使用离散核 T/V 分裂传播；连续 H0 原型与它不同，需要一致的离散核高斯传播或独立的时间步长收敛证明。
2. 将状态投影到现有 D2/D3 有界参考空间，通常会破坏单个 Slater 表示。因此不能直接替换当前控制变量的残差路径而保持原公式。
3. 解析求和电子轨道路径不消除真实时间的振荡抵消问题；仍需同参数真实模型的方差、偏差与运行时间验证。
4. `exact_hopping_branches` 的占据不变分支目前要求 |cos(g dt)| >= 0.1；项目当前小步长满足此限制，但通用大步长还需单独的极限表达式。

星形单体耦合只有一个明亮浴模式，g = sqrt(sum_j c_j^2)。当 0 < Nel < Norb 时，多体 H1 的谱范数也是 g，与电子数无关；已用上述小全空间谱独立核验。这为改进泊松提议率提供依据，但不是未经实测即可降低采样率的理由。

## 复现

- `python experiments/slater_path_kernel.py`
- `python experiments/coherent_poisson_kernel.py`
- 数值摘要：`scan_v114_slater_kernel_20260906/kernel_validation.json` 和 `coherent_poisson_validation.json`。

非正交 Slater 矩阵元背景可参见 Burton 的 [Generalised Nonorthogonal Matrix Elements](https://arxiv.org/abs/2101.10944)。上述条件跳跃与泊松权重是本项目的独立推导和数值核验；该文献不构成对本原型长时精度的证明。
