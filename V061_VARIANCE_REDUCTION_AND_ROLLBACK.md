# v0.61 泊松路径降方差修改与回退说明

## 1. 回退基线

修改前的版本已经冻结为 Git 标签：

`baseline-s10n5-oxygen-v060`

对应提交为 `93d4244`。服务器上的下列文件始终没有被覆盖：

- `ahm-mb-sep.cpp`
- `na_mpi_cloud.out`

基线哈希为：

- `ahm-mb-sep.cpp`: `843694ce9fbcca45f6cfb170163953b78a08a177b270353b329b52d08fef2849`
- `na_mpi_cloud.out`: `0feef1a464cf9bd22e76af7a64c46dc12c17f249d3c32a9d0840949fb18d6bbf`

因此在服务器上运行 `na_mpi_cloud.out` 就是在运行原来的 v0.60；新版本使用
`na_mpi_v061.out`，二者互不覆盖。

最终实验文件哈希为：

- `ahm-mb-sep-v061.cpp`: `196df81ae102c1930f690613a8d2e31dce48ea53a548bd70b513d9ea17feff0b`
- `na_mpi_v061.out`: `b39be62720be2488cc6874657dffa37dda33e4b0065402e84244551f2fd0f57a`

## 2. 原权重的公式问题

记单步无量纲耦合强度为

\[
r=\sqrt{N_{\mathrm{el}}N_{\mathrm{vac}}}\,|c|\,\Delta t,
\qquad
c=\sqrt{\frac{\eta}{N_{\mathrm{orb}}-1}}.
\]

原程序每个离散时间步使用伯努利跳跃：

\[
P(\text{jump})=r,\qquad P(\text{no jump})=1-r,
\]

但整条路径却乘连续泊松补偿

\[
s_{\mathrm{old}}=e^r.
\]

于是一步期望中的“不跳跃”系数和“跳跃”系数分别为

\[
e^r(1-r),\qquad e^r r,
\]

而目标的一阶传播算符

\[
I-iV\Delta t
\]

要求它们分别为

\[
1,\qquad r.
\]

单步误差看似只有 \(O(r^2)\)，但经过 \(j\) 步后会以乘法方式累积。对于当前
`s10n5, eta=0.01, dt=0.5`，有

\[
r=0.0833333,
\]

已经不能把这个误差当作很小的连续时间近似。

## 3. 精确且局部最优的伯努利均匀化

令跳跃概率为 \(p\)，不跳跃和跳跃时分别用重要性权重补偿。单步二阶矩的
主要部分为

\[
M_2(p)=\frac{1}{1-p}+\frac{r^2}{p}.
\]

令导数为零：

\[
\frac{dM_2}{dp}=\frac{1}{(1-p)^2}-\frac{r^2}{p^2}=0,
\]

得到

\[
p_{\mathrm{opt}}=\frac{r}{1+r}.
\]

此时两种事件可以使用同一个逐步补偿因子

\[
s=1+r.
\]

因为

\[
s(1-p)=1,
\qquad
sp=r.
\]

所以新的随机一步传播在当前离散模型内严格无偏，同时使局部二阶矩最小。
代码相应改为

\[
P(\text{jump})=\frac{r}{1+r},
\qquad
S_j=(1+r)^j=\exp[j\log(1+r)].
\]

## 4. 后向路径补偿修正

后向路径为了到达指定末态，会条件化到以下集合：

- 跳跃次数与前向路径同奇偶；
- 跳跃次数不小于到达该末态所需的最小次数；
- 电子路径最终到达指定行列式。

设前两个条件的接受概率为 \(A_j\)，到达指定末态的概率为
\(P_{\mathrm{end}}\)。条件采样后，完整的无偏补偿应为

\[
A_j P_{\mathrm{end}} S_j.
\]

原代码使用了

\[
A_j P_{\mathrm{end}}S_{j-\mathrm{offset}},
\]

其中 `offset` 是最后一次后向跳跃的位置。这只补偿最后一次跳跃之后的区间，
使不同跳跃历史获得不同的错误权重；零跳跃路径甚至没有完整后向补偿。

v0.61 改为统一使用完整的 \(S_j\)。`offset` 现在只负责计算确定性传播时间，
不再进入统计权重。

## 5. 后向跳跃阶数分层

满足奇偶和最小阶数限制时，后向跳跃次数的条件分布为

\[
q_k=
\frac{\binom{j}{k}p^k(1-p)^{j-k}}
{A_j}.
\]

新版本不再反复生成整条伯努利序列直到接受，而是直接从这个条件分布采样
\(k\)，再从 \(j\) 个位置中均匀选出 \(k\) 个跳跃时间。

当 `SEP_MB_BACK_REPLICAS=B` 时，使用随机移位分层点

\[
u_b=\left(u_0+\frac{b}{B}\right)\bmod 1,
\qquad b=0,\ldots,B-1.
\]

每个 \(u_b\) 的边缘分布仍是均匀分布，所以估计保持无偏；不同副本覆盖条件
分布的不同区域，减少多个副本全部落在同一跳跃阶数附近的情况。默认值为
`B=4`。

## 6. 前向分层开关

代码也保留了可选的全 MPI 前向跳跃阶数分层：

`SEP_MB_STRATIFY_FORWARD_COUNT=1`

100000 条轨道的验证中，它把全区间重复噪声 RMS 从 `0.00906` 降至
`0.00771`，但运行时间从约 5 秒增加到约 7 秒，方差乘时间几乎没有改善。
因此最终默认关闭，保留作后续不同参数下的实验选项。

## 7. s10n5 氧 2p 验证结果

固定参数：

\[
N_{\mathrm{orb}}=10,\quad N_{\mathrm{el}}=5,\quad
\Delta E=-0.6319\ \mathrm{Ha}=-17.194874968839816\ \mathrm{eV},
\]

\[
w_c=10\ \mathrm{eV},\qquad \eta=0.01,
\qquad t\in[0,25].
\]

每组使用 100000 条轨道并独立重复三次：

| 版本 | 全区间重复噪声 RMS | 相对精确 QM 的 RMS 误差 |
|---|---:|---:|
| v0.60 | 0.431020 | 0.257137 |
| v0.61，后向副本 1 | 0.021979 | 0.011443 |
| v0.61，后向副本 4 | 0.009063 | 0.004310 |
| v0.61，前后向分层且后向副本 4 | 0.007705 | 0.005251 |

所有版本的十个轨道占据数之和与 5 的最大偏差都小于
\(6\times10^{-12}\)。v0.61 默认选择后向副本 4、关闭前向分层。

服务器最终验证任务 `636267` 使用最终 v0.61 二进制、100000 条轨道和
\(t\in[0,25]\)，正常结束，退出码为 `0:0`。服务器只保留：

- `cloud-runs/636267-v061-final`: 最终泊松结果；
- `cloud-runs/636261`: 同参数精确 QM 对照。

## 8. 回退方法

服务器立即回到现版本：

```bash
./na_mpi_cloud.out 10 1 NTRAJ 10 5
```

本地源码回到冻结基线：

```bash
git switch main
git switch --detach baseline-s10n5-oxygen-v060
```

正常开发时不需要执行第二条；标签主要用于核对或重建完全相同的旧源码。
