# 30/15、4000 a.u. 提议速率筛选

任务数组 647806，六个子任务，最多同时一个。速率倍数 1.0、1.5、2.0，每档种子 1159001 和 1159002，100000 条路径；128 进程，每节点 64 个。每个子任务最多两小时、每节点 4 GiB。并非改变 eta、wc 或哈密顿量。

使用已有 v1.14 程序并复用 647702 的 D3/F384 参考。缓存 key 不含提议速率，但必须通过完整模型、参考空间、时间步、进程数和校验和检查。资源测量用 MPI 内部的 GNU time 包住各个真实求解器进程，process-0.txt 至 process-127.txt 记录用户 CPU 秒、系统 CPU 秒、墙钟秒和峰值 KiB；process-resources.txt 汇总。峰值之和不是同时 RSS。正式源码未因本筛选被修改。

服务器输出路径：原 nonadia 项目下 cloud-runs/rate-comparison-4000-20260907/647806_0 至 647806_5。提交命令保存在 submit_rate_comparison_4000.sh。任务清单在 jobs.tsv。

先检查完整 8001 行与各项物理参数，再比较各档两种子的噪声及相对参考的差异。参考不是完整 QM 真值，两种子也不足以给出可靠置信界；不能根据某个幸运种子的高比值宣称收敛。选出候选后还需独立大样本验证。有限样本固定格点分层的舍入偏差和有限 dt 误差仍需另行核验。
