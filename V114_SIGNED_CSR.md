# v1.14：等耦合符号压缩与首份 30/15 全 4000 a.u. 对照

## 当前成果

保持原哈密顿量、星形 Poisson/Kondo 路径、费米子符号、随机数调用顺序及浮点求和顺序。仅压缩有界参考空间的跃迁图。

等幅耦合边的源状态索引本身携带正负号：负边保存 `-(source+1)`，正边保存 `source`，整个图共用一个耦合绝对值。源状态 0 的负号不会丢失；正负零仍可精确恢复。只有各边绝对值逐位相等时才采用压缩；非等耦合保留浮点值数组，绝不近似取整为同一个强度。`SEP_MB_REFERENCE_SIGNED_CSR=0` 可回退到原图存储。缓存格式和物理解算语义不变，旧缓存读取已做云端回归。

D3 有 715136 个参考态、5261580 条有向边。图数据由 65.670 MiB/进程降到 25.527 MiB/进程，节省 40.143 MiB/进程。这个数是跃迁图组件的大小；程序总体峰值见下表。

## 实测短测

同参数 30 轨道 / 15 电子，0–10 a.u.，100 条路径，384 振动态。旧版与新版的输出逐点一致。

| 测试 | 旧版每进程峰值 MiB | 新版每进程峰值 MiB | 旧版峰值和 GiB | 新版峰值和 GiB |
| --- | ---: | ---: | ---: | ---: |
| D2，64 进程 | 30.516 | 28.336 | 1.798 | 1.653 |
| D3，64 进程 | 297.758 | 257.770 | 18.496 | 16.009 |
| D3，128 进程 | 232.410 | 192.234 | 28.824 | 23.807 |

峰值和是各进程各自最大值相加，并非同时测量的总 RSS。

短测作业 647709–647714 均成功，包括小系统、非压缩回退、D2/D3 和跨版本缓存读取。六组结果最大差异均为 0。

64 进程 D3 耗时由 253.26 s 到 242.26 s。最初新版双节点测试为 249.93 s，而旧版为 152.67 s；这两个作业没有完全相同的进程分配参数，不能据此单独归因于算法。647716 已在同一批节点上固定 64 进程/节点，以旧、新、新、旧顺序完成复测。四次输出完全一致；旧版两次参考平均 147.286 s，新版 146.400 s（比值 0.994），未见明显运行时间损失。新版两次峰值约 192.3 MiB/进程，旧版约 232.4 MiB/进程。结果由 `analyze_v114.py` 写入 `same_allocation_AB`，原始异常耗时保留。

| 同节点对照 | 旧版第一次 | 新版第一次 | 新版第二次 | 旧版第二次 |
| --- | ---: | ---: | ---: | ---: |
| 参考传播秒数 | 147.496 | 146.216 | 146.583 | 147.076 |
| 墙钟秒数 | 150.12 | 148.74 | 149.24 | 149.63 |

后续默认使用 `na_mpi_v114_signed_csr.out`。双节点运行应明确每节点 64 个进程；不要把不同放置条件的耗时差异直接归因于算法。

本地验证：45 类图、81920 条解码边、14742 次逐位 H1 比较；525 次完整参考算子比较；7 项缓存完整性检查，全部通过。

## 首份真实 30/15、0–4000 a.u. 数据

作业 647708 已完成，D2 / F384 / 128 进程 / 100 条路径。8001 个时间点完整，dt=0.5。程序墙钟 4214.96 s（约 1 h 10 min），最大进程峰值 32.703 MiB，进程峰值和 3.614 GiB。原始参考范数最大偏差 4.26e-14；最终输出粒子数最大偏差 5.51e-14。后者受到最终归一化约束，不能单独作为传播精度或幺正性的证明。

物理参数保持 wc=2.7 eV、eta=0.30e-6、delE=-17.194874968839816 eV。D2 参考的杂质最终占据变化为 -4.0605e-7；其与 100 路径最终估计的最大轨道差异为 1.798e-9。这是参考生产与阶数对照任务，不能以仅 100 路径的结果证明高拟合。

D3/F384 和 D3/F512 的 4000 a.u. 参考作业 647702、647703 仍在运行，随后按成功依赖启动 647704–647707 的百万路径采样。已运行约 2 h 38 min 时，F384 到 600 a.u.，F512 到 500 a.u.。按目前传播速度，参考全程约需 16.3 h 与 19.9 h，采样耗时另计。整体精度目标尚未证明。

`analyze_v113_large_4000.py` 仅接受完整 8001 行、正确时间网格、正确物理与实际参考参数、完成标记及正确缓存校验和的数据。它生成全部 30 轨道图，分别给出参考阶数差异、振动截断差异以及三独立种子的 SEM；最后一种只叫重复性指标，不叫精确 QM 拟合。

## 百万路径真实 QM 对照

新增同参数 10 轨道 / 5 电子、4000 a.u. 的三独立种子测试，每次 100 万路径、64 进程、最长 12 小时。参考生产 647717 已完成（Slurm 26 s），采样 647718、647719 已启动，647720 等待节点名额，三者复用 647717 的参考。对应种子 1144001–1144003；清单见 `scan_v114_signed_csr_20260906/small_4000_jobs.tsv`。`analyze_v114_small_4000.py` 对照已经完成的 647656 网格 QM，按全时间范围及逐轨道 100 a.u. 窗口评价实际 Q。这三个任务现均已完成；单次最弱活跃轨道的最差 100 a.u. 窗口 Q 分别为 11.858、11.169、16.821。完整资源数据及边界说明见 4000AU_VALIDATION_AND_50X25.md。

## 小系统报告更正

此前 647666 报告把请求的 F1024 写成实际使用值。程序将该请求限制为 F512，已按运行日志与缓存键修正说明。1024 个 QM 空间网格点是另一项参数。原始数值数据和误差结果不变。

## 独立研究原型

Slater 条件电子路径、核相干态传播和泊松计数权重已做独立核验，见 `SLATER_KERNEL_PROTOTYPE.md`。它尚未接入正式算法，尚无真实 Anderson–Holstein 模型的蒙特卡洛方差改善证明。

## 复现与回退

- `python tests/test_reference_signed_csr.py`
- `python tests/test_reference_rhs.py`
- `python tests/test_reference_cache.py`
- `python analyze_v114.py`
- `python analyze_v113_large_4000.py`
- 候选服务端二进制：`na_mpi_v114_signed_csr.out`，SHA256 `4354060f4de44e5ef7a644b35fb401111385e7e934f202c7b283589d685edda2`。
- 正在运行的 4000 a.u. 作业继续使用 v1.13 二进制，便于按完整批次分析。
- 既有本地回退点 `v1.13-two-buffer-cache`，已上传的较早回退点 `v1.13-csr-candidate`。本轮回退标签为 `v1.14-signed-csr`；已获用户授权并上传到公开 GitHub 仓库，v1.13-two-buffer-cache 和 v1.14-signed-csr 均可回退。
